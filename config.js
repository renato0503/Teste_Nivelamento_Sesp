/* ==========================================================================
   Configuração do Teste de Nivelamento - SESP (área administrativa)
   Edite este arquivo depois de publicar o app da Web no Google Apps Script.
   ========================================================================== */
window.NIV_CONFIG = {

  // URL do "App da Web" gerado pelo Google Apps Script (ver README.md).
  // Enquanto estiver vazio, o teste funciona no modo código:
  // cada aluno recebe um código e o instrutor cola os códigos no painel.
  apiUrl: "",

  // Mesmo valor definido em TOKEN dentro de apps-script.gs.
  // Serve para o painel ler as respostas.
  token: "sesp-2025",

  // Senha para abrir o painel de análise (instrutor).
  painelSenha: "sesp-dados",
};
